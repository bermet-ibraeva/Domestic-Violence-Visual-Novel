public static class TempGameContext
{
    // Текущий сейв, который нужно загрузить в диалоговой сцене
    public static SaveData saveToLoad;

    // Можно добавить — текущий эпизод (например, EpisodeData после загрузки)
    public static EpisodeData loadedEpisode;
}
